import "./character-description.style.css";

const CharacterDescription = ({ description }) => {
    return <p className="character-description">{description}</p>;
};

export default CharacterDescription;
