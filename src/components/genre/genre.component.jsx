import "./genre.style.css";

const Genre = (props) => {
    return (
        <a style={{ backgroundColor: {} }} className="genre">
            {props.genre}
        </a>
    );
};

export default Genre;
